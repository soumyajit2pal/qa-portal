{
  "name": "qualityops-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Centralized QA Portal -- single React + Vite SPA. Domain areas (Functional, Security, Specialised Testing, Governance) live as plain folders under src/modules/ and are code-split via React.lazy(), but all ship in one build/deploy -- this project previously used a Module Federation micro-frontend split (separate images per module) and was consolidated back into one app; see README 'Frontend architecture'.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs tests/token-renewal.cjs",
    "test:request-navigation": "node --test tests/requestNavigation.test.mjs",
    "test:frontend-guards": "node --test tests/frontendGuards.test.mjs",
    "test:qa-document-upload": "node --test tests/qaDocumentUpload.test.mjs",
    "preview": "vite preview --port 5173",
    "test:login-encryption": "node --test tests/loginEncryption.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "7.18.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "6.1.1",
    "typescript": "^5.5.4",
    "vite": "8.3.0"
  }
}
